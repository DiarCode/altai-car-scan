fastapi==0.115.2
uvicorn[standard]==0.30.6
numpy==2.1.2
opencv-python-headless==4.10.0.84

# If you will run ONNX exports (recommended for CPU):
onnxruntime==1.19.2

# If you keep PyTorch .pt inference:
ultralytics==8.3.50
torch>=2.2,<3.0; platform_system != "Windows"
# If Windows CPU, consider: torch==2.3.1 (CPU) from PyPI

# Angle classifier dependency
open-clip-torch==2.24.0
